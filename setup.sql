-- CREATE TABLE Shoes (
--   id int NOT NULL AUTO_INCREMENT,
--   price DECIMAL(10,2) NOT NULL,
--   brand VARCHAR(255) NOT NULL,
--   size int,
--   style VARCHAR(255),
--   PRIMARY KEY(id)
-- );

-- INSERT INTO Shoes (price, brand, size, style) VALUES (50, "Converse", 7, "Hi-top");
-- INSERT INTO Shoes (price, brand, size, style) VALUES (55, "Converse", 7, "Low-top");

-- SELECT * FROM Shoes;

SELECT * FROM Shoes WHERE id = 1;